val x = [3, 4 , 5];

val y = 2::x;

val xx = [[9]];

val yy = y::xx;
(* Check if the lsit is empty*)
null yy;

(* Head: Return the first elements of the list*)
hd x;

(* Tail: Return all elements other than the first element*)
tl x;

(* Empty list has a alpha type 'a *)
val ept = [];

val k = (2,5)::ept;
